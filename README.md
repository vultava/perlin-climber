Climber Agent that climbs across Perlin Noise Mountains!

A simple project consisting of an environment.py that is used to create the perlin map, agent.py and train.py that, using torch, create a Deep Q Neural Network, and visualizer.py that displays the path of the climber using Opencv.
